1. implement a barebones pipeline loading
DONE

2. implement character and pipeline sleection
    a. go back button
    b. upon clicking new chat, a javascript script queries all of the character card elements 
    and adds them to a class that glows upon hovering
    c. when the characters are added, then a new "add character" card is added onto the left navbar too. this is accomplished by setting the previously hidden image to visible and altering the  src image on it, and visibling the text as well.
DONE

3. implement starting a ne wchat with character and pipeline
DONE

4. logging and saving messages
DONE

5. adding characters into chat

what needs to happen is that upon clicking the button, it executes a javscript that
    a. gets a list of all of the characterIds in convo-head-imgs
    b. gets a list of all of the data-set-character-characterIds
    c. adds selectable-for-chat to all of them
    d. removes selectable-for-chat from all of the data-set-character-ids with the first list
    e. adds selected-for-chat to them instead

DONE

6. swapping pipeline

8. editing chat messages
DONE

9. add new pipeline card
DONE

10. implement agent card
DONE

11. move around agent card with mouse
    a. add event listener to each agent card, tracked by agentid, that tracks for mousedown
    b. calculate the position of the box

DONE

12. implement creating, writing to lorebook
    a. upon chat creation, a new lorebook is created in the directory, "chatlorebooks". it is an empty json.
    DONE

    b. when an agent tries to write to it, the output is passed directly to addLorebookEntries. the agent passes a series of entries in raw json
    DONE

    c. the addLorebookEntries loads the chatlorebook via pydantic object
    DONE

    d. loops through each of the entries in the passed lorebook entries, does a model validate, and appends it to the chatlorebook object
    DONE

    e. saves it to the chatlorebook
    DONE
DONE

13. accessing lorebook to different levels of specificity
    a. {name-alias-type-tags-lb}, {name-alias-type-lb}, {name-alias-lb}, {name-lb} are the injectors. 
    b. define python function that outputs first
    c.
this also necessitates adding a "write to lorebook" option in the "outputs" category of the agent.

14. writing lorebook
DONE

15. defining show to screen agent
DONE

16. defining entrypoint agent
17. loopcount
???. implement character editing in chat

