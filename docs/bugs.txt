clicking on Agent loadout brings it to selected-card class or somehting and it glows
DONE

new character card is selectable upon reacing the new chat screen
saving chat id by number is dangerous and can overwrite previous chats. use a randomly generated hash
DONE

dashed lines around tiny convo head images
you can visit chats/1/2/3/4/5 in the url
SO WHAT

clicking the + new card button permanently puts it on "selected for chat" i think. CHECK THIS
the expansion of the pipeline config does not quite match the width of the "layout" flex container that contains left, middle, and right
DEPRECATED

the svgs are too close to the borders on the agent box
DONE

the cards can be clicked from the loadout editor screen
DONE

the connector lines in the editor lie under the input for the agent
if the user clicks on the input form in the agent pane, the instructins preview goes blank. if they click away, it stays blank
make buttons fill the width of the line above in the loadout pane
compressed character images
lines do not move with the agents
DONE

title on agent should be larger and centered
make agent card title much narrower horizontally
DONE

remove line in agent card for chatmessagecount
longer names should not change the sizeof the card; the name should go upward, producing a larger black portion
only one agent pane at a time should be allowed to appear.
scrolling all the way down on the homepage, showing loadout cards and agent cards.
the loadout image creator should be more purple-themed, using the css colors.
show "saved config" toast
tokens counter does not show "tokens" label
DONE

new chat pane has lame fonts for the character and loadout
DONE

new chat pane has lame look of the  button
new character form needs to be cleared
the token count is inaccurate; it does not account for and cannot account for the input {}s
the line for the lower branch is not centered on the svg
margins on branch output
the starting/ending position of lines on the agent tabs do not update with more text being added to the instruction preview
untitled chat # title is not saved